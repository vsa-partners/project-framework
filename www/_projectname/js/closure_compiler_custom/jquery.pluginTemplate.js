/*global jQuery: true, Modernizr:true, vsa: true */

/* Author: VSA Partners
	- Jeremy Kahn
*/

(function( $ ){
	
	$.fn.plugin = function() {
		
		return this.each(function (index, el) {
			
			el = $(el);
		});
	};
}( jQuery ));